import ts from 'typescript';import fs from 'node:fs/promises';import assert from 'node:assert/strict';
const source=(await fs.readFile('lib/site-url.ts','utf8')).replace("import 'server-only';",'');const js=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.ESNext}}).outputText;const {siteUrl}=await import('data:text/javascript;base64,'+Buffer.from(js).toString('base64'));const old={mode:process.env.NODE_ENV,url:process.env.NEXT_PUBLIC_SITE_URL};
try{process.env.NODE_ENV='development';delete process.env.NEXT_PUBLIC_SITE_URL;assert.equal(siteUrl(),'http://localhost:3000');process.env.NODE_ENV='production';assert.throws(siteUrl);for(const url of ['http://example.com','https://localhost','https://127.0.0.1','https://example.com/extra','https://user:pass@example.com','https://example.com?next=bad']){process.env.NEXT_PUBLIC_SITE_URL=url;assert.throws(siteUrl)}process.env.NEXT_PUBLIC_SITE_URL='https://savannah-rental.example';assert.equal(siteUrl(),'https://savannah-rental.example');console.log('PASS: 9 production URL validation checks')}finally{if(old.mode===undefined)delete process.env.NODE_ENV;else process.env.NODE_ENV=old.mode;if(old.url===undefined)delete process.env.NEXT_PUBLIC_SITE_URL;else process.env.NEXT_PUBLIC_SITE_URL=old.url}
